

<div class="container text-center">
  <div class="row">
    <div class="col-md-6 col-md-offset-3">
    <%= form_for [@student, @comment] do |f| %>
      <%= f.text_field :comment, class: 'form-control', placeholder: 'Enter A Message' %>
      <br>
      <%= f.submit 'Add Comment', class: 'btn btn-default'%>
    <% end %>
    </div>
  </div>
</div>
